# Optional: local voice output (tts_engine.py).
# Not needed to run race_engineer.py with --no-tts, or to run the test suite.
kokoro>=0.9.0
sounddevice>=0.5.0
soundfile>=0.12.0
numpy>=1.24.0
requests>=2.31.0
